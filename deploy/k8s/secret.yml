apiVersion: v1
kind: Secret
metadata:
  name: hotel-voice-bot-secrets
  namespace: hotel-voice-bot
type: Opaque
data:
  # Base64 encoded values - replace with actual values
  SENTRY_DSN: "" # echo -n "your-sentry-dsn" | base64
  OPENAI_API_KEY: "" # echo -n "your-openai-key" | base64
  WHATSAPP_TOKEN: "" # echo -n "your-whatsapp-token" | base64
  SUPABASE_URL: "" # echo -n "your-supabase-url" | base64
  SUPABASE_KEY: "" # echo -n "your-supabase-key" | base64
  PAGERDUTY_INTEGRATION_KEY: "" # echo -n "your-pagerduty-key" | base64
